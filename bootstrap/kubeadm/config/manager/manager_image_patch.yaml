apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: gcr.io/spectro-images-public/release/cluster-api/kubeadm-bootstrap-controller:spectro-v0.3.11
          name: manager
