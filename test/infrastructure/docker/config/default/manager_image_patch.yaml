apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: gcr.io/spectro-images-public/cluster-api-docker/capd-manager-amd64:spectro-v0.4.3-20211231
        name: manager
